# plan.md — 기획 및 설계 의사결정 기록

이 문서가 만들어진 배경: "유튜브 채널 영상 평균 조회수를 쉽게 조회"하려는 요구에서 출발해,
요구사항이 바뀌고 구현 방식이 진화한 과정을 시간순으로 남긴다. (최종 운영 규칙은 `AGENTS.md` 참조)

## 0. 초기 요구
유튜브 채널의 영상 평균 조회수를 쉽게 조회. 조건:
- 최근 10개 영상 **또는** 최근 3개월 영상
- **롱폼만**
- 가능하다면 **광고만 발라내서** 계산하면 best

## 1. 방법론 검토 (무엇으로 구현할까)
- 세 조건(최근N/3개월 + 롱폼만 + 광고 분리)을 **동시에** 만족하는 원클릭 무료 도구는 사실상 없음.
  - Social Blade: 채널 평균/추이만. 롱폼·광고 자동 분리 불가.
  - ViewStats: 영상별 조회수를 표로, 무료 티어 충분. 광고 자동분리 불가, 쇼츠는 수동 제외.
  - 수동(/videos 탭 → 스프레드시트): 채널 1~2개면 최속, 확장 불가.
- 결론: **YouTube Data API v3 무료 쿼터로 직접 계산**이 유일하게 정확한 경로. (사용자는 이미 API 경험 보유)

## 2. 쿼터 최적화 설계 (핵심 아이디어)
- 흔히 쓰는 `search.list`(100유닛)를 **버린다**.
- 대신: 업로드 플레이리스트 ID(채널ID `UC`→`UU` 치환) → `playlistItems.list`(1유닛) → `videos.list`(1유닛/50개).
- 효과: 채널당 2~10유닛 수준 → 일일 10,000유닛으로 수백~수천 개 채널 가능.

## 3. 판별 규칙 정의
- 롱폼: `duration` > 180초(쇼츠가 최대 3분이므로 이 경계가 표준).
- 최근 3개월: `publishedAt` ≥ 오늘−90일. 최근 10개: 필터 통과 롱폼 중 최신 10개.
- 광고: 유튜브 API가 "유료광고" 필드를 안 주므로 **키워드 휴리스틱**(제목/설명/태그). 100% 아님 → 근거 노출로 검증 가능하게.

## 4. 1차 구현 — CLI (`cli.mjs`, 구 `yt-avg-views.mjs`)
- 단일 `.mjs`, 의존성 0(Node 내장 fetch), 모드 `both|10|3m`, `--csv`, `--json`, `--selftest`.
- 자가검증 13케이스 통과(duration 경계 179/181s, 협찬/sponsored/#ad, DOWNLOAD·ADVENTURE 오탐 방지).

## 5. 요구 변경 ① — 브라우저 로컬 웹앱으로 전환
사용자: "코드를 먼저 짜버렸구나. 웹브라우저에서 돌릴 수 있게 **환경 구축 먼저**. 로컬 전용이면 됨.
API키는 새 프로젝트로 발급 예정. 채널주소→UC→UU 변환 도구도. 디자인은 흰 배경, 안 예뻐도 됨."
- 결정: 무의존성 Node 서버(`server.mjs`)가 `127.0.0.1`에서 `index.html` 서빙 + YouTube API **프록시**.
  - 프록시 이유: 브라우저 CORS·키 referrer 제한 회피, 키를 서버에 둘 수 있음.
- 변환 도구: 채널주소(한글 퍼센트인코딩 핸들 포함) 파싱 → UC(채널ID)/UU(업로드PL) + 복사 버튼 + 플레이리스트 URL.
- 검증: 서버 기동, `/`, `/config`, 무키 400, 허용외 path 400, 한글 핸들 디코딩.

## 6. 요구 변경 ② — 광고를 "제거"가 아니라 "비교"로
사용자: "광고는 거르지 않고, 광고 콘텐츠 조회수를 더 중점적으로 보고 싶다. 일반 vs 광고 평균을
비교할 수 있게 각각 필드로 수집해도 될 것 같다."
- 결정: 전체 평균 + **일반 평균 / 광고 평균 / 광고÷일반 배율**을 나란히 출력. 영상별 분류 표로 근거 공개.

## 7. 요구 변경 ③ — 복사 버튼 위치
- 1차: "평균 조회수 나오는 곳에 복사 버튼" → 제목 옆 **요약 1개 복사**로 구현.
- 사용자 정정: "숫자 **옆에** 복사 버튼" → 각 평균 카드(전체/일반/광고/배율) 숫자마다 **개별 복사 버튼**으로 재구현.
  복사값은 쉼표 없는 원본 숫자(스프레드시트/BI 붙여넣기 용이).

## 8. 쿼터 한도 문의 대응
- 기본 10,000 유닛/일/프로젝트, 리셋 = 태평양 자정(9월 PDT 기준 KST 16시).
- 이 도구 채널당 3~20유닛 → 하루 1,000~2,000개 채널 수준. 부족 시 API 제한 설정/쿼터 연장/멀티 프로젝트.

## 9. 배포 준비 (현재 상태)
- `git init -b main`, `.gitignore`(csv/DS_Store/node_modules/.env), 초기 커밋 완료.
- `AGENTS.md`(운영 규칙)·`plan.md`(본 문서) 기록.
- **대기 중**: ① 원격 저장소 URL(사용자 제공) ② `design.md`(사용자 제공 → 반영해 `index.html` 디자인 최신화 후 커밋·push).

## 10. 향후 후보 (미확정)
- 채널 다중 입력 + 누적 유닛 사용량 표시.
- 광고 키워드 채널별 프리셋/수정 UI.
- (필요 시) 다나와 pcode류 크로스 플랫폼 병합처럼, 동일 브랜드 협찬 영상 태그 고도화.
